# Ryan_Comfy_Utils Handoff

时间：2026-07-09 12:35:28 Asia/Shanghai

## 下一会话重点（用户指定）

**生成最新交接：完整梳理项目现状 + 本次（健壮性 / Runtime 闭环）修改。**

建议：先确认是否 `git commit` 本批未提交改动，再决定真实 CLI/Comfy 联调或继续产品节点（Storyboard 等）。

## 上一版交接链

| 文档 | 说明 |
|------|------|
| [handoff-20260709-112434.md](handoff-20260709-112434.md) | 要求「重新梳理、优先健壮性、不着急实测」 |
| [handoff-20260709-104808-acp-resolver-file-output.md](handoff-20260709-104808-acp-resolver-file-output.md) | Skill Resolver + File Exporter 设计（已实现于 `5155468`） |
| [handoff-20260708-233204.md](handoff-20260708-233204.md) | 多图槽位、export_to_file、固定 Prompt/Analyze |

计划（本轮执行依据）：

- Claude plan：`~/.claude/plans/stateful-imagining-sphinx.md`（P0–P5 健壮性）
- 仓库内计划索引：`docs/superpowers/plans/2026-07-08-*.md`、`2026-07-09-acp-skill-resolver-and-file-exporter.md`

## 本会话做了什么

1. **全项目梳理**（对照 PRD/ADR）：确认分层对齐；标出 P0「context 未注入 CLI / 失败当成功」等缺口。
2. **按批准计划实施 P0–P5**（代码 + 测试 + 文档），**未 git commit**。
3. 测试：`python3 -m unittest discover -s tests -p 'test_*.py' -v` → **65 passed**（此前 45）。

### 本批核心行为变更

| 主题 | 变更 |
|------|------|
| CLI 上下文注入 | 始终写 `session/input/prompt.txt`；command 占位符展开；env `RYAN_ACP_CONTEXT_FILE` / `RYAN_ACP_SESSION_DIR` |
| 失败语义 | `returncode != 0`、超时、非法 `result.json`、`status=error\|failed` → `RuntimeError`；接入 `validate_result_payload` |
| 路径安全 | 新 `path_safety`：session_id / skill_id / export subdir / filename 防 `..` 穿越 |
| 资产落盘 | 同名加 `01_` 前缀；已在 `session/input` 内则不二次 copy |
| 图片槽位 | 去掉 `_ryan_image_staging`；槽位直接写 `session_dir/input/images` |
| 节点 DRY | `acp_nodes` ~525→470 行：`_collect_run_image_slots`、`_resolve_workspace_root`、`**image_kwargs` |
| workspace 优先级 | 节点非空 > profile.`workspace_root` > `output/acp_workspace` |

### 新增 / 主要修改文件（未入库）

**新增**

- `ryan_comfy_utils/acp/command_expand.py`
- `ryan_comfy_utils/acp/path_safety.py`
- `tests/acp/test_command_expand.py`
- `tests/acp/test_path_safety.py`
- `docs/agents/acp-runtime-cli-profile.md`

**修改（相对 `origin/main` / `5155468`）**

- Runtime / 资产 / 导出 / skill / workspace / 节点 / 多图：见 `git status` 中 `ryan_comfy_utils/**`、`tests/**`、`README.md`、契约文档
- fixture：`ryan_comfy_utils/acp/fixtures/profiles/local_codex.json` → `["codex","exec","{context}"]`

占位符与 profile 约定：**只引用** → [docs/agents/acp-runtime-cli-profile.md](../docs/agents/acp-runtime-cli-profile.md)

## 仓库当前能力快照

### ComfyUI 节点（已注册，`__init__.py`）

- **ACP：** Universal、Image/Video Prompt、Image Analyze  
- **LLM：** Chat、Vision Chat（多图槽位）  
- **File：** Ryan File Exporter  
- **其它：** Prompt Template、Batch Video Loader、Video Frame Sampler  

### ACP Runtime 关键模块

| 模块 | 路径 | 备注 |
|------|------|------|
| 协议 | `acp/contracts.py` | manifest/profile/result 校验 |
| session / workspace | `session.py`、`workspace.py` | session_id 已 sanitize |
| skill | `skill_loader.py`、`skill_resolver.py` | Fixed / Selectable |
| context | `context_builder.py`、`template_engine.py` | |
| CLI | `cli_runner.py`、`command_expand.py`、`runtime.py` | **本轮闭环重点** |
| 资产 | `asset_materializer.py`、`image_slot_paths.py` | 单树落盘 |
| 导出 | `file_exporter.py` | 节点内开关 + 独立 File 节点 |
| 路径安全 | `path_safety.py` | **本轮新增** |
| 节点适配 | `nodes/acp_nodes.py`、`comfy_image_inputs.py`、`file_nodes.py` | |
| 前端多图 | `web/ryan_multi_image_slots.js` | |

### 数据流（当前实现）

```
Node → resolve skill → prepare_workspace
     → 槽位/paths → session/input/images（或外部 path 再 materialize）
     → build context → prompt.txt + context.json
     → expand command + env → CLI
     → validate result → map fields → 可选 export
```

### 测试

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

→ **65 passed**（无 Comfy 时部分路径 lazy；tensor 落盘仍弱依赖 torch）。

### Git

- 分支：`main` 跟踪 `origin/main`
- 最新已推送提交：`5155468` feat: skill resolver + File Exporter（12–14）
- **本轮全部改动仍在工作区**（modified + untracked），**未 commit / 未 push**

## Issues / 计划状态

| 区间 | 状态 |
|------|------|
| 01–10 | Foundation + 固定 Prompt/Analyze → 已实现（历史提交） |
| 11–14 | export + Resolver + File Exporter + 文档 → 已实现（`5155468`） |
| **健壮性 P0–P5** | **代码已完成，待 commit**（无对应 issue 编号；来源为梳理结论 + plan） |

契约文档：

- `docs/agents/acp-fixed-prompt-agent-contracts.md`（Analyze 图 = 槽位或 paths）
- `docs/agents/acp-file-export-convention.md`（路径安全段）
- `docs/agents/acp-runtime-cli-profile.md`（**新**）
- `docs/agents/comfy-multi-image-inputs.md`

## 仍后置 / 已知限制

1. **真实 Claude/Codex CLI 联调** — 占位符机制已就绪；真实 argv 以联调为准。  
2. **ComfyUI 端到端手测** — 含多图 UI、export、失败中断。  
3. **LLM / Video 节点** — 几乎无单测；本轮未动。  
4. **returncode 收紧** — 依赖「非 0 仍当成功」的本地 hack 会失败（有意）。  
5. **`{context}` 过大** — 文档推荐 `{context_file}`。  
6. **manifest 的 input/output_contract / mode** — 仍偏文档预留，runtime 不强制执行 contract。  
7. **大量 untracked 规划资产** — `.planning/`、`.scratch/issues/01–09` 等、部分 `docs/agents/*`、`AGENTS.md` 未入库。  
8. Storyboard / Character 等固定节点 — 产品后续。

## 建议下一会话任务（按优先级）

1. **审阅 diff 后 commit**（可拆：runtime+tests / path+assets / nodes+docs）— 用户须明确同意再提交。  
2. 可选：用假 `command` 在本机再跑一轮「读 prompt.txt 打印」烟雾（不接真实 codex）。  
3. 真实 CLI profile 样例（Claude / Codex 各一）+ 文档补真实参数。  
4. Comfy 手测清单：固定三节点 + Universal + File Exporter + 故意失败 CLI。  
5. 是否整理提交 `.scratch` / `.planning` / `AGENTS.md`。  

## 风险与回滚

- 回滚面主要在 `runtime.py`、`path_safety` 接入点、`asset_materializer` 文件名变化（相对路径现为 `01_name`）。  
- 旧内部依赖 `_ryan_image_staging` 的路径已失效（本就非对外契约）。  
- 工作区可 `git checkout -- <paths>` 丢弃本批；新增文件需手动删。

## 建议技能（下一会话）

| 目标 | 技能 |
|------|------|
| 提交前审查 | `codereview`、`review`、`simplify` |
| 提交与分支 | `git-guardrails-claude-code`（仅在用户要求提交时） |
| 真实联调 / 手测 | `run`、`verify`、`diagnose` |
| 文档与 changelog | `document-helper` |
| 新固定节点产品 | `brainstorming` → `writing-plans` → `implement` / `tdd` |
| 架构再收敛 | `improve-codebase-architecture`（非必须） |

**不建议优先：** 在未 commit 本批健壮性改动前大开新功能分支并行改 runtime。

## 代码入口

- 包根：`ryan_comfy_utils/`
- 注册：`__init__.py` → `NODE_CLASS_MAPPINGS`
- Runtime 主路径：`ryan_comfy_utils/acp/runtime.py` → `execute_text_session`
- 节点：`ryan_comfy_utils/nodes/acp_nodes.py`
- 测试：`tests/acp/`、`tests/nodes/`

## 敏感信息

本 handoff 不含 API Key / 密钥；LLM profile 仍用环境变量（见 README）。
