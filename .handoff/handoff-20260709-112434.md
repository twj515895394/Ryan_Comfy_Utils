# Ryan_Comfy_Utils Handoff

时间：2026-07-09 11:24:34 Asia/Shanghai

## 下一会话重点（用户指定）

**对项目重新梳理一遍；不着急实测（ComfyUI / 真实 CLI 联调可后置）。优先保证代码健壮性。**

建议工作方式：以测试、架构边界、重复逻辑、错误路径、文档与 issue 一致性为主；实测与产品验收排在稳健性审查之后。

## 上一版交接链

| 文档 | 说明 |
|------|------|
| [handoff-20260708-233204.md](handoff-20260708-233204.md) | ACP Prompt/Analyze、多图槽位、`export_to_file` |
| [handoff-20260709-104808-acp-resolver-file-output.md](handoff-20260709-104808-acp-resolver-file-output.md) | File Exporter 命名、Skill Resolver 设计（**已实现于 `5155468`**） |

## 仓库当前能力快照（引用，不展开）

### ComfyUI 节点（已注册）

- **ACP：** Universal、Image/Video Prompt、Image Analyze（反推）
- **LLM：** Chat、Vision Chat（多图槽位）
- **File：** `Ryan File Exporter`（非 ACP）
- **其它：** Prompt Template、Batch Video Loader、Video Frame Sampler

### ACP Runtime 关键模块

| 模块 | 路径 |
|------|------|
| 协议 / session / runtime | `ryan_comfy_utils/acp/` |
| Skill 绑定 | `skill_resolver.py`（Fixed / Selectable） |
| 多图 + 落盘 | `nodes/comfy_image_inputs.py`、`acp/image_slot_paths.py` |
| Agent 内导出 | `acp/file_exporter.py` |
| 节点适配 | `nodes/acp_nodes.py`、`nodes/file_nodes.py` |
| 前端「+ 图片」 | `ryan_comfy_utils/web/ryan_multi_image_slots.js` |

### 测试（交接时）

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
```

→ **45 passed**（无 ComfyUI/torch 环境时部分路径为 lazy import；全量在 Comfy 环境更完整）。

### 最近提交（`origin/main`）

- `5155468` — issues **12–14**：Skill Resolver、Ryan File Exporter、文档
- `cc74a4d` — 多图槽位 + UI
- `bdbad1d` / `dbd1c0f` / `437ba4c` — Analyze、导出开关、固定 Prompt 节点

## Issues 状态（`.scratch/acp-runtime-foundation/issues/`）

| 区间 | 状态 |
|------|------|
| 01–10 | Foundation + 固定 Prompt/Analyze → **已实现** |
| 11 | export 开关 **已完成**；独立节点由 **13** 承接 |
| **12–14** | Resolver + File Exporter + 文档 → **已实现**（`5155468`） |

计划索引：

- [2026-07-08-acp-runtime-foundation.md](../docs/superpowers/plans/2026-07-08-acp-runtime-foundation.md)
- [2026-07-08-acp-fixed-prompt-agents.md](../docs/superpowers/plans/2026-07-08-acp-fixed-prompt-agents.md)
- [2026-07-09-acp-skill-resolver-and-file-exporter.md](../docs/superpowers/plans/2026-07-09-acp-skill-resolver-and-file-exporter.md)

契约 / 约定：

- `docs/agents/acp-fixed-prompt-agent-contracts.md`
- `docs/agents/acp-file-export-convention.md`
- `docs/agents/comfy-multi-image-inputs.md`

## 建议的「重新梳理 / 健壮性」清单（非实测）

1. **测试覆盖缺口**
   - `resolve_image_inputs_for_acp` / tensor 落盘（需 torch 或 fake tensor 策略）
   - Universal Agent + resolver 边界（manifest 空 skill、错误 skill_root）
   - `write_text_export` json 非法输入、路径遍历 `output_subdir`
2. **架构一致性**
   - `skill_loader` vs `skill_resolver` 是否还有节点直连 loader
   - `acp_nodes.py` 体积与重复 `run()` 参数（10 个 image_xx）是否值得收敛
3. **静态与约定**
   - README / 契约 与 `INPUT_TYPES` 是否一致
   - issue 验收标准勾选状态（可在 issue 评论标 completed）
4. **未入库规划资产**（可选整理提交）
   - `.planning/`、部分 `.scratch/issues/01-09`、`AGENTS.md`、`docs/agents/` 等
5. **刻意后置**（用户说不着急）
   - 真实 Claude/Codex CLI 联调
   - ComfyUI 端到端手测
   - Storyboard / Character 固定节点

## 代码入口（梳理用）

- 包根：`ryan_comfy_utils/`
- 注册：`__init__.py` → `NODE_CLASS_MAPPINGS`
- 测试：`tests/acp/`、`tests/nodes/`

## 风险与已知限制

- 无 ComfyUI 时 `folder_paths` 回退 `cwd/output`
- ACP 单测多 mock `execute_text_session`，集成链未覆盖真实 CLI
- `acp_nodes.run()` 签名冗长，改接口易漏测

## 建议技能（下一会话）

| 目标 | 技能 |
|------|------|
| 全库梳理与边界 | `project-analyzer`、`improve-codebase-architecture` |
| 健壮性 / 审查 | `codereview`、`review`、`karpathy-guidelines` |
| 补测试 | `tdd` |
| 问题定位（修完再测） | `diagnose` |
| 文档与 issue 对齐 | `document-helper` |
| 计划拆分新梳理项 | `writing-plans`、`to-issues` |
| 实现修复项 | `implement`（在梳理结论确认后） |

**不建议优先：** 直接 `run` / Comfy 实测，除非梳理后明确列为 P0。