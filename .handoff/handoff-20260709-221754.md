# Ryan_Comfy_Utils Handoff

时间：2026-07-09 22:17:54（本地会话）

用户请求：生成最新 handoff，供下一会话接续。

## 上一版交接

- [handoff-20260709-123528.md](handoff-20260709-123528.md) — ACP Runtime 健壮性 P0–P5、65 项单测

## 本会话完成事项（摘要）

### 1. ACP 节点 UI 与多图槽位

- **后端**：`acp_nodes.py` 精简 optional 输入（去掉 `style`/`subject`/`scene`/`task`/`extra_prompt`/`image_paths` 等）；默认 `image_slot_count=2`；`build_image_slot_input_types(include_paths=False)`。
- **前端**：`ryan_multi_image_slots.js` 通过裁剪 `node.inputs` + `_all_inputs` 备份 + `configure` 钩子，新建节点默认 **2** 个图片插槽，`Update` 按 `image_slot_count` 展开（最多 10）。
- **测试**：`test_acp_*_node.py` 已同步；全量 `unittest discover -s tests` 仍为 **65 passed**（本会话多次验证）。

计划索引（已批准）：`~/.claude/plans/valiant-exploring-crane.md`（ACP + Prompt Template 部分）。

### 2. Prompt Template 节点

- 从 `ComfyUI_Qwen3-VL-Instruct-utils/prompts` **复制** 约 22 个 `.txt` 模板到 `ryan_comfy_utils/prompts/`（与既有 `video_frame_analysis.md` 并存；**多数尚未 git add**）。
- 新增 `ryan_comfy_utils/web/ryan_prompt_template.js`：`template_source` 为「内置模板」时 **隐藏** `prompt_dir`；选「自定义目录」时显示。
- `prompt_nodes.py`：`template_source` 中文选项（内置模板 / 自定义目录）。

### 3. LLM 配置（与 ACP 分离）

- 运行时读取：`ryan_comfy_utils/config/llm_profiles.json`（**已 gitignore**，仅本机）；模板：`llm_profiles.example.json`。
- **已 push**：`8d16d69`（曾提交占位配置）、`263dfbe`（仓库删除 `llm_profiles.json` 并恢复 ignore）。
- 用户本机已配置本地 OpenAI 兼容网关（`base_url` 带 `/v1`）、默认 profile 名与模型 id；**勿在文档或提交中写入真实 api_key**。

### 4. ACP 默认改为 Claude CLI

- 新增 `ryan_comfy_utils/acp/fixtures/profiles/local_claude_cli.json`：`claude -p "{context}"`。
- `DEFAULT_PROFILE_PATH` → `local_claude_cli.json`（`acp_nodes.py`）；`local_codex.json` 保留可选。
- 文档：`docs/agents/acp-runtime-cli-profile.md` 已更新默认说明。
- `tests/acp/test_contracts.py` 改为断言 `claude_cli` profile。

**前置**：本机需已安装并可用 `claude` CLI；ACP **不**使用 `llm_profiles.json`。

### 5. 其它工作区改动（未提交，见 `git status`）

可能来自更早会话或并行编辑，下一会话需核对是否一并提交：

- `video_nodes.py` + `ryan_video_loader.js`（视频预览路由 `/ryan_comfy_utils/view_video`）
- `__init__.py`、`llm_nodes.py`、`comfy_image_inputs.py` 等
- `.scratch/comfy-node-ux/`（未跟踪）

## Git 状态（交接时点）

- **已 push `origin/main`**：`263dfbe`（LLM 配置 ignore 流程）、此前 `8d16d69`、`213ba4a` 等。
- **未提交**：ACP UI 精简、Claude 默认 profile、`ryan_prompt_template.js`、prompts 文本、文档与多文件 M 状态；`local_claude_cli.json` 为 **??**。

建议下一会话先：`git status` → 用户确认是否分 commit（例如 `feat(acp): default claude cli + node ui` / `feat(prompt): templates + prompt_dir widget`）。

## 验证命令

```bash
/x/ComfyUI-aki-v2/python/python.exe -m unittest discover -s tests
```

ComfyUI 联调：重启后检查 ACP 节点（2 槽位、无冗余字段）、Prompt Template（内置模板列表、`prompt_dir` 显隐）、ACP 跑通需 `claude` 在 PATH。

## 未完成 / 风险

| 项 | 说明 |
|----|------|
| ACP 真机联调 | 未在本会话用 ComfyUI + 真实 `claude` 跑完整 skill 链路 |
| `claude -p` 参数 | 若本机 CLI 版本与默认 `command` 不一致，需按 `claude --help` 调整 `local_claude_cli.json` |
| Prompt 模板入库 | 大量中文 `.txt` 是否全部纳入版本库需产品/用户决定 |
| LLM 密钥 | 仅本地 `llm_profiles.json`；克隆仓库后需从 `llm_profiles.example.json` 复制并自行填写 |

## 下一会话建议优先级

1. 整理并提交本会话未入库改动（含 `local_claude_cli.json`）。
2. ComfyUI 实测 ACP（Claude CLI）+ LLM 节点（本地网关 profile）。
3. 若 ACP 输出格式与 skill 契约不符，对照 `docs/agents/acp-fixed-prompt-agent-contracts.md` 与 `result.json` 约定调试。

## 建议技能

| 场景 | 技能 |
|------|------|
| 编码规范与最小改动 | `custom/karpathy-guidelines-zh` |
| 提交前审查 | `custom/codereview` |
| Comfy / 节点联调与 API | `custom/api-tester` |
| 多步骤实施与追踪 | `custom/writing-plans`、`custom/planning-with-files` |
| 架构或大范围重构前 | `engineering/improve-codebase-architecture` |
| 写变更说明 / 契约补充 | `custom/document-helper` |

## 关键路径速查

| 用途 | 路径 |
|------|------|
| ACP 默认 CLI profile | `ryan_comfy_utils/acp/fixtures/profiles/local_claude_cli.json` |
| LLM profiles（本地） | `ryan_comfy_utils/config/llm_profiles.json`（gitignore） |
| 内置 prompt 模板目录 | `ryan_comfy_utils/prompts/` |
| 多图槽位前端 | `ryan_comfy_utils/web/ryan_multi_image_slots.js` |
| Prompt 节点前端 | `ryan_comfy_utils/web/ryan_prompt_template.js` |
| ACP CLI 约定 | `docs/agents/acp-runtime-cli-profile.md` |