# Ryan_Comfy_Utils Handoff

时间：2026-07-08 23:32:04 Asia/Shanghai（修订：2026-07-08 多图槽位 `cc74a4d`）

## 本次会话目标

- 对照交接与设计文档，补齐固定 Prompt Agent 的 **issues + 实施计划** 并 **实现**（07–09）。
- 确认第二批固定 Agent 选型（**Image Analyze 反推** + **File Generator/导出** 约定），实现 issue 10/11（部分）。
- 为 **Image/Video Prompt** 节点补齐 **`export_to_file`** 界面开关。
- **统一多图输入**：`image_01`…`image_10`，默认 1 路，界面 **「Ryan + 图片」** 最多 10 路（ACP + Vision）。
- 生成本交接文档，供下一会话接续。

## 本次会话已完成（按提交）

| 提交 | 摘要 |
|------|------|
| `437ba4c` | 固定 **Image / Video Prompt** Agent：共享 manifest/skill、`run_fixed_acp_agent`、节点注册与测试 |
| `bdbad1d` | **Image Analyze** 反推节点、`image_prompt_reverse` skill、`file_exporter`、Analyze 上 `export_to_file` |
| `dbd1c0f` | Image / Video **Prompt** 节点增加 `export_to_file`，路径 `image_prompt` / `video_prompt` |
| `cc74a4d` | **多图槽位** `image_01`…`image_10` + `ryan_multi_image_slots.js`（+ / − 按钮）；ACP tensor 落盘；Vision Chat 迁移 |

**测试：** `python3 -m unittest discover -s tests -p 'test_*.py' -v` → **38 passed**（含多图约定单测；落盘路径依赖 ComfyUI 环境时在运行期测）。

## 多图输入（全节点标准）

- 约定文档：`docs/agents/comfy-multi-image-inputs.md`
- 实现：`ryan_comfy_utils/nodes/comfy_image_inputs.py`
- 前端：`ryan_comfy_utils/web/ryan_multi_image_slots.js`（**刷新 ComfyUI** 后生效）
- 适用：**Universal / Image·Video Prompt / Image Analyze**、**Ryan LLM Vision Chat**
- ACP 仍可选 **`image_paths`**，与槽位 PNG 落盘（`workspace/_ryan_image_staging/<session_id>/`）合并进 runtime

## 当前 ACP 节点一览（ComfyUI）

| 显示名 | 角色 |
|--------|------|
| Ryan ACP Universal Agent | 可选 skill，探索入口 |
| Ryan Image Prompt Agent | 从意图**创作**图像 prompt |
| Ryan Video Prompt Agent | 创作视频 prompt |
| Ryan Image Analyze Agent | 从参考图**反推** prompt（7 类 `category`，默认双语） |

**导出（BOOLEAN，默认关）：** 开启后写入 `output/ryan_acp_exports/<node_slug>/`。`node_slug`：`image_analyze` / `image_prompt` / `video_prompt`。

## 规划与 issue 状态（引用，不展开正文）

- PRD：`.scratch/acp-runtime-foundation/PRD.md`
- 选型（含第二批 Analyze）：`.scratch/acp-runtime-foundation/fixed-agent-node-selection.md`
- Issues **01–05**：Foundation（已实现）
- **06**：选型 HITL → 07–09
- **07–09**：Prompt 固定节点 → **已实现**（`437ba4c`）
- **10**：Image Analyze → **已实现**（`bdbad1d`）
- **11**：导出 → **部分**（`file_exporter` + 三节点开关；**独立 File Generator Agent 未做**）

实施计划：

- `docs/superpowers/plans/2026-07-08-acp-runtime-foundation.md`
- `docs/superpowers/plans/2026-07-08-acp-fixed-prompt-agents.md`

契约：

- `docs/agents/acp-fixed-prompt-agent-contracts.md`
- `docs/agents/acp-file-export-convention.md`

反推参考资料（Obsidian Clippings）：  
`~/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian/Clippings/全网最全「反推提示词」指南：再也不用到处抄Prompt了！.md`

## 待开发 / 建议下一刀

1. **Issue 11 剩余**：独立 **Ryan File Generator Agent**。
2. **真实 CLI**：Codex/Claude 联调。
3. **暂缓固定**：Storyboard、Character Design（Universal）。
4. 可选：`exported_file_path` 作为 ComfyUI 输出；Video/其他非 ACP 节点是否也要多图槽位。

## 代码入口

- 节点：`ryan_comfy_utils/nodes/acp_nodes.py`、`llm_nodes.py`
- 多图：`ryan_comfy_utils/nodes/comfy_image_inputs.py`
- 导出：`ryan_comfy_utils/acp/file_exporter.py`
- Runtime：`ryan_comfy_utils/acp/runtime.py`

## 仓库状态

- 分支 `main`，本会话 feat 已提交；交接修订后 **push 至 origin**（见当次 `git push` 结果）。
- 未跟踪规划资产可能仍含：`.planning/`、部分 `.scratch/issues/`、`docs/superpowers/`、`AGENTS.md` 等。

## 上一版交接

- `handoff-20260708-170042.md`（首批 Prompt 待实现，已 supersede）
- 本文件为 **233204** 修订版（含 `cc74a4d`）

## 建议技能

- `implement` — File Generator 节点 / CLI 联调
- `handoff`
- `writing-plans` / `document-helper`
- `karpathy-guidelines` / `review`